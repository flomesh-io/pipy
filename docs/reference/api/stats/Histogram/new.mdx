---
title: "Histogram()"
api: stats.Histogram.new()
---

# Syntax

``` js
new stats.Histogram(name, [...buckets])
new stats.Histogram(name, [...buckets], [...labelNames])
```

## Parameters

<Parameters/>
