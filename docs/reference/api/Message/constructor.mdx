---
title: "Message()"
api: Message.constructor()
---

# Syntax

``` js
new Message()
new Message(text)
new Message(data)
new Message(head, text)
new Message(head, data)
```

## Parameters

<Parameters/>
