---
title: "Configuration.fork()"
api: Configuration.fork()
---

# Syntax

``` js
pipy().pipeline('example').fork(target)
pipy().pipeline('example').fork(target, { ...initialValues })
pipy().pipeline('example').fork(target, [ ...initialValueObjects ])
pipy().pipeline('example').fork(target, () => { ...initialValues })
pipy().pipeline('example').fork(target, () => [ ...initialValueObjects ])
```

## Parameters

<Parameters/>

## Return value

<ReturnValue/>
