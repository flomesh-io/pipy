---
title: "Configuration.handleMessageStart()"
api: Configuration.handleMessageStart()
---

## Description

Appends a **handleMessageStart** filter to the current pipeline layout.

A **handleMessageStart** filter calls a user function every time it sees a [MessageStart](/reference/api/MessageStart) event in the input stream. Its input and output can be any kinds of events.

<FilterDiagram
  name="handleMessageStart"
  input="Event"
  output="Event"
/>

The user function that gets called will have a [MessageStart](/reference/api/MessageStart) object as parameter.

This filter passes every input event down to its output. It doesn't change how many or what events there are. However, since events are all just objects being passed around by reference, if you alter the event object itself, the change will be visible on the output, even when the references to the events are still identical between the input and the output.

## Syntax

``` js
pipy()
  .pipeline('example')
    .handleMessageStart(
      event => onMessageStart(event)
    )
```

### Parameters

<Parameters/>

### Return value

<ReturnValue/>

## See Also

* [Configuration](/reference/api/Configuration)
* [handleData()](/reference/api/Configuration/handleData)
* [handleStreamStart()](/reference/api/Configuration/handleStreamStart)
* [handleStreamEnd()](/reference/api/Configuration/handleStreamEnd)
* [handleMessageBody()](/reference/api/Configuration/handleMessageBody)
* [handleMessageEnd()](/reference/api/Configuration/handleMessageEnd)
* [handleMessage()](/reference/api/Configuration/handleMessage)
