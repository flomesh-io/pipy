---
title: "Configuration.connect()"
api: Configuration.connect()
---

# Syntax

``` js
pipy().pipeline('example').connect(target)
pipy().pipeline('example').connect(target, { ...options })
pipy().pipeline('example').connect(() => target)
pipy().pipeline('example').connect(() => target, { ...options })
```

## Parameters

<Parameters/>

## Return value

<ReturnValue/>
