---
title: "Configuration.detectProtocol()"
api: Configuration.detectProtocol()
---

# Syntax

``` js
pipy().pipeline('example').detectProtocol(
  (protocolName) => onProtocolDetected()
)
```

## Parameters

<Parameters/>

## Return value

<ReturnValue/>
