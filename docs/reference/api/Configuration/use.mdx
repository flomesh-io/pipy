---
title: "Configuration.use()"
api: Configuration.use()
---

# Syntax

``` js
pipy().pipeline('example').use(filename, pipelineName)
pipy().pipeline('example').use(filename, pipilineName, downPipelineName)
pipy().pipeline('example').use(
  filename,
  pipilineName,
  downPipelineName,
  () => shouldTurnDown()
)
```

## Parameters

<Parameters/>

## Return value

<ReturnValue/>
