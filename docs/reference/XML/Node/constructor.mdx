---
title: "Node()"
api: XML.Node.constructor()
---

# Syntax

``` js
new XML.Node(name)
new XML.Node(name, { ...attributes })
new XML.Node(name, { ...attributes }, [ ...children ])
```

# Parameters

<Parameters/>
