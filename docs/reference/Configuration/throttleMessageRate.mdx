---
title: "Configuration.throttleMessageRate()"
api: Configuration.throttleMessageRate()
---

# Syntax

``` js
pipy().pipeline('example').throttleMessageRate(limit)
pipy().pipeline('example').throttleMessageRate(limit, account)
```

## Parameters

<Parameters/>

## Return value

<ReturnValue/>
