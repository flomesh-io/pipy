---
title: "Cache()"
api: algo.Cache.constructor()
---

# Syntax

``` js
new algo.Cache(
  (k) => add(k)
)

new algo.Cache(
  (k) => add(k),
  (k, v) => remove(k, v)
)
```

## Parameters

<Parameters/>
