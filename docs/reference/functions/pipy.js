/**
 * Create a new Configuration object.
 *
 * @param {Object.<string, *>} [globals] Global variables and their initial values.
 * @returns {Configuration} A new Configuration object.
 */
function pipy(globals) {}

/**
 * Read a file's raw content from the codebase.
 *
 * @param {string} path Path name of the file to read.
 * @returns {Data} Raw data of the file content.
 */
pipy.load = function(path) {}
