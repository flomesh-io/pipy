---
title: 17 数据格式转换
---

## 问题

JSON 被广泛应用于 Web 开发，作为应用端与服务端交互的数据格式。在 JSON 之前，有很多交互使用 XML 格式。可能一些遗留遗留系统还在使用 XML 格式，甚至短期还要继续保留。这种系统假如要继续对应用端提供服务，免不了需要二次开发。假如我们的代理可以完成转换的操作，就可以方便遗留系统的快速接入。

对，这次我们就为代理扩展数据格式转换的能力。